#pragma once#include <Drag.h>extern Boolean		gBalloonHelp;			// Balloon Help vorhanden?extern Boolean		gDrapAndDrop;			// Drag & Drop Manager vorhanden?extern short		gSystemVersion;			// Versionsnummer vom Systemextern short		gQDVersion;				// Haupt-Quickdraw-Version:											//	0: originales QD											//	1: Color QD											//	2: 32-bit QDextern OSType		gSignature;				// Signatur der Applikationextern Str31		gAppName;				// Name des Programmsextern short		gCurrentCursor;			// aktueller Cursorvoid			SetMinimumStack(long minSize);short			NumToolboxTraps(void);TrapType		GetTrapType(short theTrap);Boolean			TrapAvailable(short theTrap);long			GetGestaltResult(OSType gestaltSelector);void			GlobalToLocalRect(Rect *aRect);void			LocalToGlobalRect(Rect *aRect);Boolean			AbortInQueue(void);Boolean			CmdPeriod(EventRecord *theEvent);Boolean			IsFrontProcess(void);Boolean			IsRunningUnderAUX(void);void			InitAll(void);/*** *	verschiendene Cursorformen setzen ***/#define pfeil()			SetAnimCursor(0)#define biene()			SetAnimCursor(watchCursor)#define textmaus()		SetAnimCursor(iBeamCursor)void			SetAnimCursor(short resID);void			FetchCursor(short id);void			SpinCursor(short increment);#define kDITop					0x0050		// kTopLeft - for positioning the Disk Initialization dialogs.#define kDILeft					0x0070