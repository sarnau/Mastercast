/*** *	Klasse zum offscreen Zeichnen * *	©1994 Dekotec Abt. Info-Sys, Markus Fritze ***/#pragma once#include "GlobalDefines.h"#include <QDOffscreen.h>class OffscreenObject {	CGrafPtr	port;	// geretteter Grafport	GDHandle	gdh;	// gerettete Device	GWorldPtr	gw;		// eigene Offscreen-GWorld	GrafPtr		gwind;	// gemerktes Window des Konstruktors	Boolean		failed;	// Offscreen-Bitmap allozierbar? Nein, dann wird halt normal gezeichnet	Rect		srcrect;	// Rechteck in Windowkoordinate, das gezeichnet werden soll	Rect		destrect;	// Zielrechteck (Ÿblich: gleichgro§ mit srcrect)public:	OffscreenObject(GrafPtr iWindow, Rect *iRect);	// Offscreen-Bitmap erzeugen	~OffscreenObject();								// der Destructor kopiert sie dann auf den Bildschirm!	void		SetDestRect(Rect *iRect);			// neues Zielrechteck setzen	Boolean		Failed();							// Init hat geklappt? true, wenn nicht!};