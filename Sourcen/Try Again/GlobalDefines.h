/*** *	Hier landen wirklich GLOBALE Defines… ***/#pragma once/*** *	Define freigeben, wenn eine Beta-Test-Version erstellt wird. ***/#define DEBUG			0#if DEBUG// Fehlermeldungen geben einen Alert o.ä. aus#define Debug_Throw// Signal() - sowas wie assert() - ausführen, wenn definiert#define Debug_Signal#endif/*** *	true, wenn eine Demoversion erzeugt werden soll ***/#define DEMO			0#if __profile__	#include <profiler.h>#endif/*** *	Damit man die Warnungen des Compilers verhindern kann… ***/#define UNUSED(unused) do { unused; } while(0)/*** *	PowerPlant Includes einfügen (wg. dem Exception Handler) ***/#include <PP_Prefix.h>