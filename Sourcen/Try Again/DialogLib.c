/*** *	Ex-Library fŸr Dialoge ***/#if 0/*** *	Dialogbox in passender Grš§e fŸr den Bildschirm suchen * *	Dabei wird von maxID bis zu theID _abwŠrts_ gesucht! D.h. maxID ist die grš§te Dialogbox, *	theID ist die kleinste, die _STETS_ pa§t! Au§er theID kšnnen alle grš§eren Dialogboxen auch *	fehlen, es wird dann die nŠchst kleinere genommen. ***/int			GetFitDialog(int theID, int maxID, Rect *r){Handle	theTemplate;					// Handle to resource templateRect		*iRect;int			i;	for(i = maxID; i>theID; i--) {		theTemplate = GetResource('DLOG',i);		// DLOG-Resource einlesen		if(!theTemplate || ResError())				// Resource nicht vorhanden			continue;								// => wird ignoriert		iRect = (Rect*)*theTemplate;				// Rechteck der Dialogbox (Grš§e!)		if((iRect->right - iRect->left) > (r->right - r->left))			continue;								// Dialogbox zu breit?		if((iRect->bottom - iRect->top) > (r->bottom - r->top))			continue;								// Dialogbox zu hoch?		break;										// Dialogbox pa§t! => raus	}	return i;}#endif